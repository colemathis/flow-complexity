So with ms04 we tried extending the range for k_d in order to have a clearer view of the peak in molecular complexity, however we’re quickly running into calculation time issues (takes multiple days to complete).

Here we’ll want to run a few simulations just to check what’s the dependency on M.